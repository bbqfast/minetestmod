Todo:
- farmer replant on the same spot if seed avaiable



1. One‑shot clean (what you already used)
[IO.File]::WriteAllText('.\debug_clean.txt', -join (Get-Content .\debug.txt -Encoding Byte | Where-Object { $_ -ne 0 } | ForEach-Object { [char]$_ }))


2. Stream tail → clean → file (live, tail‑like)
Get-Content .\debug.txt -Encoding Byte -Wait | Where-Object { $_ -ne 0 } | ForEach-Object { [char]$_ } | Out-File .\debug_clean_stream.txt -Encoding utf8 -NoNewline

3. Stream tail → clean → screen
Get-Content .\debug.txt -Encoding Byte -Wait | Where-Object { $_ -ne 0 } | ForEach-Object { [char]$_ } | Write-Host -NoNewline


- one line tail but from begining

Get-Content .\debug.txt -Encoding Byte -Wait | Where-Object { $_ -ne 0 } | ForEach-Object { [char]$_ } | Tee-Object -FilePath .\debug_clean_stream.txt | Write-Host -NoNewline



One‑liner: only updates, cleaned, with copy to file
Get-Content .\debug.txt -Encoding Byte -Tail 0 -Wait | Where-Object { $_ -ne 0 } | ForEach-Object { [char]$_ } | Tee-Object -FilePath .\debug_clean_stream.txt | Write-Host -NoNewline


tail -n0 -F debug.txt | tr -d '\000' | tee debug_clean_stream.txt
tail -n0 -F debug.txt | tr -d '\000'




------------------------------
create a function 

remaining_craftable_output takes the following argument
craftable_outputs
desired_outputs

Returns 
craftable_outputs minus everything in 

---------------------------
create a function that takes
1. craftable_outputs

output a structure that can represent craftable output
that can be used to fill the formspec
note that it need to be able to track which slot is empty because user can drag item out of craftable inventory

something that like an array like
{item1, item2, <empty>, item3, item4}



---------------------------



Updates:
- consolidate log stmt
- farmer core now only need 1 layer fence
- cooker teleport to path if too far
- cooker do not cross fence, and teleport back if stuck inside fence line
- refactor furnace 
- furance always take, refuel
- furnace lower than 20 coal check
- attach sword to ltangel 
- farmer replant on the same spot if seed avaiable


todo:
- fix sitting maidroid  moving
